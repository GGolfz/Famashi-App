import 'package:flutter/material.dart';

class MedicineEditScreen extends StatelessWidget {
  static String routeName = '/medicine-edit';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Medicine Edit Screen"),
      ),
    );
  }
}
