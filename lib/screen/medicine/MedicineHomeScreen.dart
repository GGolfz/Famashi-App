import 'package:flutter/material.dart';

class MedicineHomeScreen extends StatelessWidget {
  static String routeName = '/medicine';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Medicine Home Screen"),
      ),
    );
  }
}
