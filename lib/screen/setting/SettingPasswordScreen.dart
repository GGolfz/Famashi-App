import 'package:flutter/material.dart';

class SettingPasswordScreen extends StatelessWidget {
  static String routeName = '/setting-password';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Setting Password Screen"),
      ),
    );
  }
}
